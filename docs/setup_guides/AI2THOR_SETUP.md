# AI2-THOR Setup

Install:

```bash
pip install ai2thor
```

Run on a machine with a graphical display or X server. The Floor 6 executor targets `FloorPlan6`; if rendering is unavailable, use dry-run mode for repeatable evidence.

